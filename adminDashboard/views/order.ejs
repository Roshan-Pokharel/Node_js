<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheet/output.css">
  <title>Your Orders | StyleStore</title>
</head>
<body class="bg-slate-50 min-h-screen text-slate-800 font-sans">
  
  <nav class="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-slate-200 shadow-sm">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <a href="/dashboard">
          <div class="flex items-center gap-3">
            <div class="h-10 w-10 bg-indigo-600 rounded-lg flex items-center justify-center text-white font-bold text-lg">S</div>
            <span class="hidden md:block text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-600 to-violet-600">
              StyleStore
            </span>
          </div>
        </a>
        <div class="hidden md:flex items-center gap-8 mr-65">
          <a href="/dashboard" class="text-sm font-medium text-slate-500 hover:text-indigo-600 transition-colors">Home</a>
          <div class="relative group"> 
    <button id="catBtn" class="text-sm font-medium text-slate-500 hover:text-indigo-600 flex items-center gap-1 transition-colors cursor-pointer">
        Categories
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
    </button>

    <div id="catPanel" class="hidden absolute top-full mt-2 left-1/2 -translate-x-1/2 w-48 bg-white shadow-lg border border-slate-200 p-3 rounded-xl z-50">
        <% 
           let uniqueCategories = new Set(); 
           /* IMPORTANT: We use 'productData' here, not 'products', 
              because 'products' only contains what is inside the cart */
           if (typeof productData !== 'undefined') {
               productData.forEach(p => uniqueCategories.add(p.category));
           }
        %>
        <div class="space-y-1">
            <% uniqueCategories.forEach(cat => { %>
                <a href="/categories/<%= cat %>" class="block px-3 py-2 rounded-lg text-sm text-slate-700 hover:bg-slate-50 hover:text-indigo-600 transition">
                    <%= cat %>
                </a>
            <% }) %>
        </div>
    </div>
          </div>
          <a href="/orders" class="text-sm font-medium text-indigo-600 border-b-2 border-indigo-600 pb-1">Orders</a>
           <a href="/product/cart" class="text-sm font-medium text-slate-500 hover:text-indigo-600 flex items-center gap-1 transition-colors cursor-pointer">
            Cart
            <span class="bg-indigo-100 text-slate-500 text-[10px] font-bold px-2 py-1 rounded-full"><%= totalQuantity %></span>
          </a>
        </div>
        <div class="flex items-center space-x-4">
          <div class="flex items-center gap-3">
            <div class="h-9 w-9 rounded-full bg-slate-200 flex items-center justify-center text-slate-600 font-semibold text-sm">U</div>
            <a href="/logout" class="hidden md:block text-sm font-medium text-slate-500 hover:text-red-500 transition-colors">Logout</a>
          </div>
           <button class="md:hidden p-2 text-slate-600 hover:bg-slate-100 rounded-lg" id="mobile-menu-button">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
          </button>
        </div>
      </div>
    </div>
    <div class="hidden md:hidden border-t border-slate-200 bg-white" id="mobile-menu">
      <div class="px-4 pt-2 pb-4 space-y-1">
        <a href="/dashboard" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Home</a>
        <a href="/categories" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Categories</a>
        <a href="/orders" class="block px-3 py-2 rounded-md text-base font-medium text-indigo-600 bg-indigo-50">Orders</a>
        <a href="/product/cart" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Cart</a>
        <a href="/logout" class="block px-3 py-2 rounded-md text-base font-medium text-red-500 hover:bg-red-50">Logout</a>
      </div>
    </div>
  </nav>

  <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-10">

    <div class="flex items-end justify-between mb-8">
        <div>
            <h1 class="text-3xl font-bold text-slate-900">Order History</h1>
            <p class="text-slate-500 mt-1">Check the status of your recent orders</p>
        </div>
    </div>

    <% if (Object.keys(grouped).length === 0) { %>

        <div class="text-center py-24 bg-white rounded-2xl border border-dashed border-slate-300">
            <div class="bg-slate-50 p-6 rounded-full inline-flex mb-6">
                <svg class="w-10 h-10 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
                </svg>
            </div>
            <h3 class="text-xl font-bold text-slate-900">No orders found</h3>
            <p class="text-slate-500 mt-2 mb-8">It looks like you haven't placed any orders yet.</p>
            <a href="/dashboard" class="inline-flex items-center px-6 py-3 bg-indigo-600 text-white font-medium rounded-xl hover:bg-indigo-700 transition shadow-lg shadow-indigo-200">
                Start Shopping
            </a>
        </div>

    <% } else { %>

        <div class="space-y-8">
            <% Object.keys(grouped).reverse().forEach(dateTime => { %> <div class="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden">
                
                <div class="bg-slate-50 px-6 py-4 border-b border-slate-200 flex flex-col sm:flex-row justify-between sm:items-center gap-4">
                    <div class="flex gap-8 text-sm">
                        <div>
                            <span class="block text-slate-500 font-medium text-xs uppercase tracking-wide">Order Placed</span>
                            <span class="font-semibold text-slate-900"><%= dateTime %></span>
                        </div>
                        <div>
                            <span class="block text-slate-500 font-medium text-xs uppercase tracking-wide">Total Amount</span>
                            <span class="font-bold text-indigo-600">Rs. <%= Math.floor(grouped[dateTime].totalCost) %></span>
                        </div>
                    </div>
                    
                    <a href="/order/cancel/<%= dateTime%>">
                    <div class="flex items-center gap-3" >
                         <button class=" cursor-pointer text-sm font-medium text-slate-600 hover:text-rose-600 transition-colors">
                            Cancel Delivery
                         </button>
                    </div>
                    </a>
                </div>

                <div class="divide-y divide-slate-100">
                    <% grouped[dateTime].items.forEach(order => { %>
                        <div class="p-6 flex flex-col sm:flex-row gap-6 hover:bg-slate-50/50 transition-colors group">
                            
                            <div class="w-24 h-24 flex-shrink-0 bg-slate-100 rounded-lg overflow-hidden border border-slate-200">
                                <% if(order.productId && order.productId.image && order.productId.image.length > 0) { %>
                                    <img src="data:<%= order.productId.image[0].contentType %>;base64,<%= order.productId.image[0].data.toString('base64') %>"
                                        class="w-full h-full object-cover object-center group-hover:scale-110 transition-transform duration-500" 
                                        alt="<%= order.productId.productname %>"/>
                                <% } else { %>
                                    <div class="flex items-center justify-center h-full text-slate-400 text-xs">No Image</div>
                                <% } %>
                            </div>

                            <div class="flex-1 min-w-0">
                                <div class="flex flex-col sm:flex-row justify-between items-start gap-4">
                                    <div>
                                        <h3 class="text-lg font-bold text-slate-900 hover:text-indigo-600 transition-colors truncate">
                                            <%= order.productId ? order.productId.productname : 'Product Unavailable' %>
                                        </h3>
                                        <div class="mt-1 flex flex-wrap gap-2 text-sm text-slate-500">
                                            <span class="inline-flex items-center px-2 py-0.5 rounded-md bg-slate-100 text-slate-700 text-xs">
                                                Color: <%= order.color %>
                                            </span>
                                            <span class="inline-flex items-center px-2 py-0.5 rounded-md bg-slate-100 text-slate-700 text-xs">
                                                Qty: <%= order.quantity %>
                                            </span>
                                        </div>
                                    </div>
                                    
                                    <div class="text-right">
                                        <p class="font-bold text-slate-900">Rs. <%= Math.floor(order.finalPrice) %></p>
                                        <% if(order.discountAtPurchase > 0) { %>
                                            <p class="text-xs text-green-600">Saved <%= order.discountAtPurchase %>%</p>
                                        <% } %>
                                    </div>
                                </div>

                                <div class="mt-4 flex flex-col sm:flex-row justify-between items-center gap-4">
                                    <div class="w-full sm:w-auto">
                                        <% if(order.status === "pending"){ %>
                                            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-medium bg-amber-50 text-amber-700 border border-amber-200">
                                                <span class="w-2 h-2 rounded-full bg-amber-500 animate-pulse"></span>
                                                Processing
                                            </span>
                                        <% } else if(order.status === "shipped"){ %>
                                            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-medium bg-blue-50 text-blue-700 border border-blue-200">
                                                <span class="w-2 h-2 rounded-full bg-blue-500"></span>
                                                Shipped
                                            </span>
                                        <% } else if(order.status === "delivered"){ %>
                                            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-medium bg-emerald-50 text-emerald-700 border border-emerald-200">
                                                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                                Delivered
                                            </span>
                                        <% } else { %>
                                            <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-medium bg-rose-50 text-rose-700 border border-rose-200">
                                                Cancelled
                                            </span>
                                        <% } %>
                                    </div>

                                    <% if(order.productId) { %>
                                    <a href="/product/detail/<%= order.productId._id %>" 
                                       class="w-full sm:w-auto text-center px-4 py-2 border border-slate-300 rounded-lg text-sm font-medium text-slate-700 hover:bg-slate-50 hover:text-indigo-600 transition-colors">
                                        Buy Again
                                    </a>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
            <% }) %>
        </div>

    <% } %>
  </div>

  <script src="/javascript/mobileNabBar.js"></script>
  <script src="/javascript/categories.js"></script>
</body>
</html>