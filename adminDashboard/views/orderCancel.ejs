<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheet/output.css">
  <title>Your Orders | DurgaGrocery</title>
</head>
<body class="bg-slate-50 min-h-screen text-slate-800 font-sans">
  
  <nav class="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-slate-200 shadow-sm">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <a href="/dashboard">
          <div class="flex items-center gap-3">
            <div class="h-10 w-10 bg-indigo-600 rounded-lg flex items-center justify-center text-white font-bold text-lg">S</div>
            <span class="hidden md:block text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-600 to-violet-600">
              DurgaGrocery
            </span>
          </div>
        </a>
        <div class="hidden md:flex items-center space-x-8">
          <a href="/dashboard" class="text-sm font-medium text-slate-600 hover:text-indigo-600 transition-colors">Home</a>
          <a href="/categories" class="text-sm font-medium text-slate-600 hover:text-indigo-600 transition-colors">Categories</a>
          <a href="/orders"class="text-sm font-medium text-slate-600 hover:text-indigo-600 transition-colors">Orders</a>
          <a href="/product/cart" class="text-sm font-medium text-slate-600 hover:text-indigo-600 transition-colors">Cart</a>
        </div>
        <div class="flex items-center space-x-4">
          <div class="flex items-center gap-3">
            <div class="h-9 w-9 rounded-full bg-slate-200 flex items-center justify-center text-slate-600 font-semibold text-sm">U</div>
            <a href="/logout" class="hidden md:block text-sm font-medium text-slate-500 hover:text-red-500 transition-colors">Logout</a>
          </div>
           <button class="md:hidden p-2 text-slate-600 hover:bg-slate-100 rounded-lg" id="mobile-menu-button">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
          </button>
        </div>
      </div>
    </div>
    <div class="hidden md:hidden border-t border-slate-200 bg-white" id="mobile-menu">
      <div class="px-4 pt-2 pb-4 space-y-1">
        <a href="/dashboard" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Home</a>
        <a href="/categories" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Categories</a>
        <a href="/orders" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Orders</a>
        <a href="/product/cart" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Cart</a>
        <a href="/logout" class="block px-3 py-2 rounded-md text-base font-medium text-red-500 hover:bg-red-50">Logout</a>
      </div>
    </div>
  </nav>

      <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-10">
    <div class="max-w-3xl mx-auto">
      <h1 class="text-3xl font-bold text-slate-900 mb-6">Confirm Order Cancellation</h1>
      
      <div class="bg-white rounded-xl shadow-lg border border-slate-200 p-8">
        
        <p class="text-lg text-slate-700 mb-4">You are about to cancel the order placed on: 
            <span class="font-bold text-indigo-600"><%= orderTime %></span>
        </p>

        <h2 class="text-xl font-semibold text-slate-900 mb-4 border-b pb-2">Items to be Cancelled:</h2>
        
        <div class="divide-y divide-slate-100 mb-6">
            <% orders.forEach(order => { %>
                <div class="py-3 flex justify-between items-center text-sm">
                    <div class="flex-1">
                        <span class="font-medium text-slate-900">
                           <%= order.productId ? order.productId.productname : 'Product Unavailable' %>
                        </span>
                        <span class="text-slate-500 ml-2">
                           (Color: <%= order.color %> | Qty: <%= order.quantity %>)
                        </span>
                    </div>
                    <span class="font-bold text-slate-700">Rs. <%= Math.floor(order.finalPrice) %></span>
                </div>
            <% }) %>
        </div>

        <form action="/order/cancelled/<%= orderTime %>" method="POST">
            <div class="mb-6">
                <label for="reason" class="block text-sm font-medium text-slate-700 mb-2">Reason for Cancellation (Required)</label>
                <textarea id="reason" name="reason" rows="4" required
                    class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:ring-indigo-500 focus:border-indigo-500 transition-shadow shadow-sm"
                    placeholder="E.g., Ordered wrong size, found better deal, no longer needed."></textarea>
            </div>

            <div class="flex justify-end gap-3">
                <a href="/orders" class="px-6 py-3 border border-slate-300 rounded-xl text-sm font-medium text-slate-700 hover:bg-slate-50 transition-colors">
                    Go Back
                </a>
                <button type="submit" 
                    class="px-6 py-3 bg-red-600 text-white font-medium rounded-xl hover:bg-red-700 transition shadow-lg shadow-red-200">
                    Confirm Cancellation
                </button>
            </div>
        </form>

      </div>
    </div>
  </div>
  
  <script src="/javascript/mobileNabBar.js"></script>
</body>
</html>