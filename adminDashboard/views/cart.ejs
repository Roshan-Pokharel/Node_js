<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheet/output.css">
  <title>Shopping Cart | StyleStore</title>
</head>
<body class="bg-gray-50 font-sans text-slate-800">

  <nav class="sticky top-0 z-50 bg-white/90 backdrop-blur-md border-b border-slate-200 shadow-sm transition-all duration-300">
   <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <a href="/dashboard">
          <div class="flex items-center gap-3">
            <div class="h-10 w-10 bg-indigo-600 rounded-lg flex items-center justify-center text-white font-bold text-lg">S</div>
            <span class="hidden md:block text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-600 to-violet-600">
              StyleStore
            </span>
          </div>
        </a>
        
        <div class="hidden md:flex items-center gap-8 mr-71">
          <a href="/dashboard" class="text-sm font-medium text-slate-500 hover:text-indigo-600 transition-colors">Home</a>
           <div class="relative group"> 
    <button id="catBtn" class="text-sm font-medium text-slate-500 hover:text-indigo-600 flex items-center gap-1 transition-colors cursor-pointer">
        Categories
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
    </button>

    <div id="catPanel" class="hidden absolute top-full mt-2 left-1/2 -translate-x-1/2 w-48 bg-white shadow-lg border border-slate-200 p-3 rounded-xl z-50">
        <% 
           let uniqueCategories = new Set(); 
           /* IMPORTANT: We use 'productData' here, not 'products', 
              because 'products' only contains what is inside the cart */
           if (typeof productData !== 'undefined') {
               productData.forEach(p => uniqueCategories.add(p.category));
           }
        %>
        <div class="space-y-1">
            <% uniqueCategories.forEach(cat => { %>
                <a href="/categories/<%= cat %>" class="block px-3 py-2 rounded-lg text-sm text-slate-700 hover:bg-slate-50 hover:text-indigo-600 transition">
                    <%= cat %>
                </a>
            <% }) %>
        </div>
    </div>
    </div>
          <a href="/orders" class="text-sm font-medium text-slate-500 hover:text-indigo-600 transition-colors">Orders</a>
          <a href="/product/cart"  class="text-sm font-medium text-slate-500 hover:text-indigo-600 transition-colors">
            Cart
            <span class="absolute bg-indigo-100 text-indigo-600 text-[10px] font-bold px-2 py-1 top-5 ml-1 rounded-full"><%= products.length %></span>
          </a>
        </div>

        <div class="flex items-center space-x-4">
            <div class="flex items-center gap-3">
                <div class="h-9 w-9 rounded-full bg-slate-100 border border-slate-200 flex items-center justify-center text-slate-600 font-semibold text-sm">U</div>
                <a href="/logout" class="hidden md:block text-sm font-medium text-slate-500 hover:text-red-500 transition-colors">Logout</a>
            </div>
             <button class="md:hidden p-2 text-slate-600 hover:bg-slate-100 rounded-lg" id="mobile-menu-button">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
          </button>
        </div>
      </div>
    </div>
    <div class="hidden md:hidden border-t border-slate-200 bg-white" id="mobile-menu">
      <div class="px-4 pt-2 pb-4 space-y-1">
        <a href="/dashboard" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Home</a>
        <a href="/categories" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Categories</a>
        <a href="/orders" class="block px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:bg-slate-50">Orders</a>
        <a href="/product/cart" class="block px-3 py-2 rounded-md text-base font-medium text-indigo-600 bg-indigo-50">Cart (<%= products.length %>)</a>
        <a href="/logout" class="block px-3 py-2 rounded-md text-base font-medium text-red-500 hover:bg-red-50">Logout</a>
      </div>
    </div>
  </nav>

  <main class="container mx-auto px-4 sm:px-6 lg:px-8 py-10 min-h-[calc(100vh-64px)]">

    <div class="flex flex-col md:flex-row md:items-center justify-between mb-8 gap-4">
      <div>
        <h1 class="text-3xl font-bold text-slate-900 tracking-tight">Shopping Cart</h1>
        <p class="text-slate-500 mt-1">
            <% if (products.length > 0) { %>
                You have <%= products.length %> item<%= products.length > 1 ? 's' : '' %> in your cart
            <% } else { %>
                Your cart is currently empty
            <% } %>
        </p>
      </div>
      <% if (products.length > 0) { %>
      <a href="/dashboard" class="inline-flex items-center text-sm font-medium text-indigo-600 hover:text-indigo-700 transition-colors group">
        <svg class="w-4 h-4 mr-1 group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
        Continue Shopping
      </a>
      <% } %>
    </div>

    <% if (products.length === 0) { %>
      <div class="flex flex-col items-center justify-center py-24 bg-white rounded-3xl border border-dashed border-slate-300 text-center">
        <div class="h-24 w-24 bg-indigo-50 text-indigo-600 rounded-full flex items-center justify-center mb-6 shadow-inner">
          <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path></svg>
        </div>
        <h2 class="text-2xl font-bold text-slate-900 mb-2">Your cart is empty</h2>
        <p class="text-slate-500 max-w-sm mb-8">Looks like you haven't made your choice yet. Explore our categories to find your style.</p>
        <a href="/dashboard" class="px-8 py-3 bg-indigo-600 text-white font-semibold rounded-xl hover:bg-indigo-700 transition-all shadow-lg shadow-indigo-200 transform hover:-translate-y-1">Start Shopping</a>
      </div>
    <% } else { %>
      
      <div class="flex flex-col lg:flex-row gap-8 xl:gap-12 relative">
        
        <div class="flex-1 space-y-6">
            
            <% 
               let subtotal = 0;
               let totalDiscount = 0;
            %>

            <% products.forEach((product) => { 
                const originalTotal = Math.floor(product.price * product.quantity);
                const discountAmount = Math.floor(originalTotal * (product.discount / 100));
                const finalPrice = Math.floor(originalTotal - discountAmount);
                
                subtotal += originalTotal;
                totalDiscount += discountAmount;
            %>
            
            <div class="group bg-white rounded-2xl p-4 sm:p-6 border border-slate-100 shadow-sm hover:shadow-md transition-shadow relative overflow-hidden">
              
              <a href="/cart/remove/<%= product.color %>/<%= product._id %>" class="absolute top-4 right-4 text-slate-300 hover:text-rose-500 transition-colors p-1 rounded-md hover:bg-rose-50" title="Remove Item">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
              </a>

              <div class="flex gap-4 sm:gap-6">
                <div class="w-24 h-24 sm:w-32 sm:h-32 flex-shrink-0 bg-slate-50 rounded-xl overflow-hidden border border-slate-100 relative">
                  <% if (product.image[0] && product.image[0].data) { %>
                    <img class="w-full h-full object-contain p-2 group-hover:scale-105 transition-transform duration-500"
                        src="data:<%= product.image[0].contentType %>;base64,<%= product.image[0].data.toString('base64') %>"
                        alt="<%= product.productname %>">
                  <% } else { %>
                    <div class="flex items-center justify-center h-full text-slate-300 text-xs">No Image</div>
                  <% } %>
                  
                  <% if(product.discount > 0) { %>
                    <div class="absolute top-0 left-0 bg-rose-500 text-white text-[10px] font-bold px-2 py-1 rounded-br-lg z-10">
                      -<%=product.discount %>%
                    </div>
                  <% } %>
                </div>

                <div class="flex-1 flex flex-col justify-between py-1">
                  <div>
                    <div class="flex justify-between items-start pr-8">
                        <h3 class="text-lg font-bold text-slate-900 leading-tight mb-1 cursor-pointer hover:text-indigo-600 transition-colors">
                            <%= product.productname %>
                        </h3>
                    </div>
                    
                    <div class="flex flex-wrap items-center gap-3 mt-2 text-sm">
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-slate-100 text-slate-800">
                           Color: <span class="ml-1 w-3 h-3 rounded-full bg-<%= product.color %>-500 border border-slate-300 inline-block"></span> 
                           <span class="ml-1 capitalize"><%= product.color %></span>
                        </span>
                        <span class="text-slate-500">Unit: Rs. <%= product.price %></span>
                    </div>
                  </div>

                  <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-4 mt-4">
                    
                    <div class="flex items-center">
                        <div class="flex items-center bg-slate-50 rounded-lg border border-slate-200 shadow-sm h-9">
                            <button class="minusBtn w-8 h-full flex items-center justify-center text-slate-500 hover:text-indigo-600 hover:bg-indigo-50 rounded-l-lg transition-colors" 
                                data-id="<%= product._id %>" data-color="<%= product.color %>">
                                <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="3" viewBox="0 0 24 24"><path d="M20 12H4"></path></svg>
                            </button>
                            <input class="quantityInput w-10 bg-transparent text-center text-sm font-bold text-slate-700 focus:outline-none" 
                                data-id="<%= product._id %>" data-color="<%= product.color %>"
                                value="<%= product.quantity %>" readonly>
                            <button class="plusBtn w-8 h-full flex items-center justify-center text-slate-500 hover:text-indigo-600 hover:bg-indigo-50 rounded-r-lg transition-colors" 
                                data-id="<%= product._id %>" data-color="<%= product.color %>">
                                <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="3" viewBox="0 0 24 24"><path d="M12 4v16m8-8H4"></path></svg>
                            </button>
                        </div>
                    </div>

                    <div class="text-right">
                      <% if(product.discount > 0) { %>
                        <p class="text-xs text-slate-400 line-through">Rs. <%= Math.floor(originalTotal.toFixed(2)) %></p>
                      <% } %>
                      <p class="text-xl font-bold text-slate-900 tracking-tight">Rs. <%= Math.floor(finalPrice.toFixed(2)) %></p>
                    </div>

                  </div>
                </div>
              </div>
            </div>
            <% }) %>
        </div>

        <div class="lg:w-96 flex-shrink-0">
          <div class="bg-white rounded-2xl shadow-lg shadow-slate-200/50 border border-slate-100 p-6 sticky top-24">
            <h2 class="text-lg font-bold text-slate-900 mb-6 flex items-center gap-2">
                Order Summary
            </h2>
            
            <div class="space-y-4 text-sm">
              <div class="flex justify-between text-slate-600">
                <span>Subtotal</span>
                <span class="font-medium text-slate-900">Rs. <%= subtotal %></span>
              </div>
              
              <% if(totalDiscount > 0) { %>
              <div class="flex justify-between text-emerald-600 bg-emerald-50 p-2 rounded-lg -mx-2">
                <span>Total Savings</span>
                <span class="font-bold">- Rs. <%= Math.floor(totalDiscount) %></span>
              </div>
              <% } %>

              <div class="flex justify-between text-slate-600">
                <span>Shipping Estimate</span>
                <span class="text-slate-400 text-xs mt-0.5">Calculated at next step</span>
              </div>
              
              <div class="flex justify-between text-slate-600">
                <span>Tax Estimate</span>
                <span class="text-slate-400 text-xs mt-0.5">Included in price</span>
              </div>
              
              <div class="h-px bg-slate-200 my-4"></div>

              <div class="flex justify-between items-end">
                <span class="text-base font-bold text-slate-900">Total Amount</span>
                <div class="text-right">
                    <span class="block text-3xl font-extrabold text-indigo-600 leading-none">
                        Rs. <%= Math.floor(subtotal - totalDiscount) %>
                    </span>
                    <span class="text-[10px] text-slate-400 font-medium">including VAT</span>
                </div>
              </div>
            </div> 

            <a href="/checkout" class="mt-8 w-full bg-slate-900 hover:bg-indigo-600 text-white font-bold py-4 px-6 rounded-xl flex items-center justify-center gap-2 transition-all shadow-lg shadow-slate-300 transform hover:-translate-y-1">
                <span>Proceed to Checkout</span>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg>
            </a>
            
            <div class="mt-6 space-y-3">
                <div class="flex items-center gap-3 text-xs text-slate-500 justify-center">
                    <svg class="w-4 h-4 text-emerald-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path></svg>
                    <span>Secure Encrypted Checkout</span>
                </div>
                <div class="flex justify-center gap-2 grayscale opacity-40">
                   <div class="h-6 w-10 bg-slate-200 rounded border border-slate-300"></div>
                   <div class="h-6 w-10 bg-slate-200 rounded border border-slate-300"></div>
                   <div class="h-6 w-10 bg-slate-200 rounded border border-slate-300"></div>
                </div>
            </div>
          </div>
        </div>
        
      </div>
    <% } %>
  </main>

  <script src="/javascript/mobileNabBar.js"></script>
  <script src="/javascript/quantity.js"></script>
  <script src="/javascript/categories.js"></script>
</body>
</html>